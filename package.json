{
  "name": "civicform-middleware",
  "version": "1.0.0",
  "description": "Middleware to talk to civicform frontend and backend.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hono/zod-validator": "^0.7.0",
    "hono": "^4.8.4"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
